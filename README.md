When a company has vacancy for a post it receives loads and loads of applications and it's very difficult to go through all the applications. Through this project a company can filter the applications based on the cosine similarity and only those applications are approved whose similarity exceeds the threshold value that is decided by the user else the applications are simply rejected.
